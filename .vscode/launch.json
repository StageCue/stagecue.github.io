{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch StageCue",
            "type": "pwa-chrome",
            "request": "launch",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "Start HTTP Server"
        }
    ]
}